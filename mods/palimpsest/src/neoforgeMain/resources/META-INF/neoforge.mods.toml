modLoader = "kotlinforforge"
loaderVersion = "[6,)"
license = "WTFNMFPL"

[[mods]]
modId = "${modId}"
version = "${modVersion}"
displayName = "${modName}"
authors = "fopwoc"
description = "Client-side world map that remembers every moment it has seen."

[[dependencies.${modId}]]
modId = "knhcore"
type = "required"
versionRange = "[0,)"
ordering = "AFTER"
side = "CLIENT"

[[dependencies.${modId}]]
modId = "minecraft"
type = "required"
versionRange = "[${minecraftVersion}]"
side = "CLIENT"
