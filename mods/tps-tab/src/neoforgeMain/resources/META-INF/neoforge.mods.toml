modLoader = "kotlinforforge"
loaderVersion = "[6,)"
license = "WTFNMFPL"
issueTrackerURL = "${issuesUrl}"

[[mods]]
modId = "${modId}"
version = "${modVersion}"
displayName = "${modName}"
authors = "fopwoc"
description = "Real server and dimension TPS and tick time while you hold Tab."
displayURL = "${repositoryUrl}"

[[dependencies.${modId}]]
modId = "knhcore"
type = "required"
versionRange = "[${modVersion}]"
side = "BOTH"

[[dependencies.${modId}]]
modId = "minecraft"
type = "required"
versionRange = "[${minecraftVersion}]"
side = "BOTH"
